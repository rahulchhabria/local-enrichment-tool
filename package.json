{
  "name": "local-enrichment-tool",
  "version": "1.0.0",
  "description": "AI-powered company enrichment tool — enrich any domain with firmographic, technographic, and hiring data",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "enrich": "tsx src/scripts/enrich.ts",
    "electron:compile": "tsc -p electron/tsconfig.json",
    "electron:dev": "npm run build && npm run electron:compile && electron dist/electron/main.js",
    "electron:build": "npm run build && npm run electron:compile && electron-builder --mac",
    "electron:dist": "npm run build && npm run electron:compile && electron-builder --mac --universal"
  },
  "keywords": [
    "enrichment",
    "company-data",
    "firmographic",
    "technographic",
    "ai"
  ],
  "dependencies": {
    "@ai-sdk/anthropic": "^1.0.0",
    "@sentry/node": "^10.38.0",
    "ai": "^4.0.0",
    "axios": "^1.13.4",
    "cheerio": "^1.2.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "@types/wait-on": "^5.3.4",
    "electron": "^33.4.11",
    "electron-builder": "^25.1.8",
    "electron-squirrel-startup": "^1.0.1",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3",
    "wait-on": "^8.0.5"
  }
}
